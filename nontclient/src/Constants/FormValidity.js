// The form is unchanged or the validity has not been checked.
export const DEFAULT = "DEFAULT";

// The form is being changed. That is, the user is typing in the form.
export const CHANGING = "CHANGING";

// The value in the form is valid.
export const VALID = "VALID";

// The value in the form is invalid.
export const INVALID = "INVALID";

// The value in the form already exists in the database.
// This should be used only when the value must be unique.
export const EXIST = "EXIST";

// The value in the form is empty (length of string == 0).
export const EMPTY = "EMPTY";
